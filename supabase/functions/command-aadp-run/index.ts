import { corsHeaders, db, json, parseBody, recordEvent, requireDashboardAuth, requireServiceRole } from '../_shared/command.ts';
import { createTaskGraph, runAadpTask, validateAssignment } from '../_shared/aadp.ts';
type J=Record<string,unknown>;const R=(v:unknown):J=>v&&typeof v==='object'&&!Array.isArray(v)?v as J:{};const T=(v:unknown)=>typeof v==='string'?v.trim():v==null?'':String(v);async function auth(req:Request){const s=requireServiceRole(req);return s?await requireDashboardAuth(req):null;}
async function stageProjection(runId:string,a:J){const tasks=await db(`command_tasks?run_id=eq.${runId}&select=*&order=created_at.asc`),ars=await db(`acquisition_runs?command_run_id=eq.${runId}&select=id&order=created_at.desc&limit=1`),ar=ars?.[0]?.id??null;const map:Record<string,string>={PUBLISHER_ASSIGNMENT_CREATE:'PUBLISHER ASSIGNMENT',ACQUISITION_RUN_START:'ACQUISITION START',ACQUISITION_PAGE_FETCH:'RECORD RETRIEVAL',ACQUISITION_RECORD_STORE:'RAW STORAGE',ACQUISITION_RUN_CLOSE:'ACQUISITION CLOSE',RECORD_NORMALIZATION:'POSTGRESQL PROCESSING',RECORD_DEDUPLICATION:'VERSION AND DUPLICATE CONTROL',RECORD_QUALIFICATION:'CONTRACT QUALIFICATION',QUALIFIED_RECORD_UPSERT:'NEW CONTRACT APPROVED TO ADD',REJECTION_RECORD_CREATE:'TERMINAL DISPOSITIONS',RUN_RECONCILIATION:'RECONCILIATION',PROCUREMENT_LANGUAGE_ANALYSIS:'CONTRACT ANALYSIS',AOIE_BATCH_REVIEW:'AOIE REVIEW',MATCHING_RECOMMENDATION_CREATE:'CHANGE OR NO CHANGE',MATCHING_RECOMMENDATION_TEST:'RECOMMENDATION TESTING',EXECUTIVE_REPORT_CREATE:'EXECUTIVE REPORT'};for(const task of tasks){const ds=task.state==='COMPLETED'?'COMPLETED':task.state==='RUNNING'?'IN PROGRESS':['READY','ASSIGNED'].includes(task.state)?'QUEUED':task.state==='RETRY_PENDING'?'ACTION NEEDED':['ESCALATED','FAILED'].includes(task.state)?'FAILED':task.state==='CANCELLED'?'CANCELLED':'NOT STARTED',attempts=Number(task.output_payload?.attempt_count??0),records=Number(Object.values(R(task.measurable_result)).find(v=>typeof v==='number')??0);await db('aadp_process_stage_projection',{method:'POST',headers:{Prefer:'resolution=merge-duplicates,return=representation'},body:JSON.stringify({command_run_id:runId,acquisition_run_id:ar,publisher_id:a.publisher_id,publisher_name:a.publisher_name,stage_key:task.task_type,display_name:map[task.task_type]??String(task.task_type).replaceAll('_',' '),display_state:ds,started_at:task.started_at,completed_at:task.completed_at,records_processed:records,warning_count:task.state==='RETRY_PENDING'?1:0,failure_count:['FAILED','ESCALATED'].includes(task.state)?1:0,retry_count:Math.max(0,attempts-1),evidence:{task_id:task.id,measurable_result:task.measurable_result,execution_evidence:task.execution_evidence},updated_at:new Date().toISOString()})});}}
async function semantic(runId:string){const x=await db('rpc/aadp_validate_semantic_completion',{method:'POST',body:JSON.stringify({p_command_run_id:runId})});return Array.isArray(x)?x[0]:x;}async function stopped(runId:string){const r=(await db(`command_runs?id=eq.${runId}&select=status,stop_requested_at`))?.[0];return !!r?.stop_requested_at||String(r?.status||'').toLowerCase()==='stopping';}async function cancel(runId:string){const now=new Date().toISOString();await db(`command_tasks?run_id=eq.${runId}&state=in.(READY,RETRY_PENDING,ASSIGNED)`,{method:'PATCH',body:JSON.stringify({state:'CANCELLED',completed_at:now,execution_evidence:{cancelled_by:'OPERATOR_STOP',cancelled_at:now}})});}
async function idle(now=new Date().toISOString()){await db('system_status?singleton=eq.true',{method:'PATCH',body:JSON.stringify({current_execution_state:'idle',updated_at:now})}).catch(()=>null);}async function finalizeStop(runId:string,a:J){const now=new Date().toISOString();await cancel(runId);await db(`command_runs?id=eq.${runId}`,{method:'PATCH',body:JSON.stringify({status:'stopped',aadp_state:'PAUSED',current_stage:'STOPPED_BY_OPERATOR',completed_at:now,last_activity_at:now,action_required:false,result_summary:'Stopped by operator request.'})});await stageProjection(runId,a);await idle(now);await recordEvent(runId,null,'AADP_RUN_STOPPED','AADP run stopped by operator request',{stop_propagated:true});}
async function execute(runId:string,a:J){let terminal=false,n=0;const started=new Date().toISOString();await db(`command_runs?id=eq.${runId}`,{method:'PATCH',body:JSON.stringify({status:'running',aadp_state:'RUNNING',started_at:started,last_activity_at:started})});await db('system_status?singleton=eq.true',{method:'PATCH',body:JSON.stringify({current_execution_state:'running',updated_at:started})}).catch(()=>null);while(n++<100){if(await stopped(runId))return await finalizeStop(runId,a);const ready=await db(`command_tasks?run_id=eq.${runId}&state=in.(READY,RETRY_PENDING)&select=*&order=created_at.asc&limit=1`);if(!ready.length)break;const task=ready[0];if(task.state==='RETRY_PENDING'&&task.scheduled_for&&new Date(task.scheduled_for).getTime()>Date.now()){await new Promise(r=>setTimeout(r,Math.min(1000,new Date(task.scheduled_for).getTime()-Date.now())));continue;}if(await stopped(runId))return await finalizeStop(runId,a);await db(`command_runs?id=eq.${runId}`,{method:'PATCH',body:JSON.stringify({current_stage:task.task_type,last_activity_at:new Date().toISOString()})});const outcome=await runAadpTask(runId,task,a);await stageProjection(runId,a);if(await stopped(runId))return await finalizeStop(runId,a);if(!outcome.ok&&!outcome.retry){terminal=true;break;}}const remaining=await db(`command_tasks?run_id=eq.${runId}&state=not.in.(COMPLETED,CANCELLED)&select=id,state,task_type`);let sem:J={};if(!terminal&&!remaining.length)sem=R(await semantic(runId));const complete=sem.valid===true,aadp=terminal?'ESCALATED':complete?'COMPLETED':remaining.length?'PARTIALLY_COMPLETE':'PAUSED',status=terminal?'failed':complete?'completed':'completed_with_failures',now=new Date().toISOString();await db(`command_runs?id=eq.${runId}`,{method:'PATCH',body:JSON.stringify({aadp_state:aadp,status,completed_at:complete||terminal?now:null,current_stage:null,last_activity_at:now,action_required:terminal||(!complete&&remaining.length>0),execution_evidence:{assignment_id:a.id,architecture:'AADP-OS-V1.2-STOP-AWARE',remaining_tasks:remaining,semantic_completion:sem}})});await stageProjection(runId,a);await idle(now);await recordEvent(runId,null,complete?'AADP_RUN_SEMANTICALLY_COMPLETE':terminal?'AADP_RUN_FAILED':'AADP_RUN_ACTION_NEEDED',`AADP run finished as ${aadp}`,{remaining_tasks:remaining.length,semantic_completion:sem});}
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:corsHeaders});const e=await auth(req);if(e)return e;if(req.method!=='POST')return json({error:'Method not allowed'},405);try{const b=R(await parseBody(req)),resume=T(b.resume_run_id);let run:J,a:J;if(resume){run=(await db(`command_runs?id=eq.${resume}&select=*`))?.[0];if(!run)return json({error:'Command run not found'},404);a=(await db(`publisher_assignments?id=eq.${run.publisher_assignment_id}&select=*`))?.[0];if(!a)return json({error:'Publisher assignment not found'},404);validateAssignment(a as any);const interrupted=await db(`command_tasks?run_id=eq.${resume}&state=in.(RETRY_PENDING,ESCALATED,FAILED,READY,CANCELLED)&select=*&order=created_at.asc&limit=1`),resumeStage=interrupted?.[0]?.task_type??null;if(interrupted?.[0]&&['ESCALATED','FAILED','CANCELLED'].includes(interrupted[0].state))await db(`command_tasks?id=eq.${interrupted[0].id}`,{method:'PATCH',body:JSON.stringify({state:'RETRY_PENDING',scheduled_for:new Date().toISOString(),completed_at:null})});await db(`command_runs?id=eq.${resume}`,{method:'PATCH',body:JSON.stringify({aadp_state:'RUNNING',status:'running',stop_requested_at:null,resume_source_stage:resumeStage,resumed_at:new Date().toISOString(),completed_at:null,action_required:false})});await recordEvent(resume,null,'AADP_RUN_RESUMED','AADP publisher run resumed from last unresolved stage',{resume_source_stage:resumeStage});}else{const assignmentId=T(b.assignment_id);if(!assignmentId)return json({error:'assignment_id is required'},400);a=(await db(`publisher_assignments?id=eq.${assignmentId}&select=*`))?.[0];if(!a)return json({error:'Publisher assignment not found'},404);validateAssignment(a as any);const idem=T(b.idempotency_key)||`aadp:${assignmentId}:${new Date().toISOString().slice(0,10)}`,existing=await db(`command_runs?idempotency_key=eq.${encodeURIComponent(idem)}&select=*`);if(existing.length)return json({run_id:existing[0].id,status:existing[0].aadp_state,idempotent_replay:true},202);const defs=await db('command_definitions?command_key=eq.AADP_PUBLISHER_ACQUISITION&select=id&order=updated_at.desc&limit=1'),created=await db('command_runs',{method:'POST',body:JSON.stringify({idempotency_key:idem,definition_id:defs?.[0]?.id??null,publisher_assignment_id:assignmentId,status:'queued',aadp_state:'QUEUED',current_stage:'PUBLISHER_ASSIGNMENT_CREATE',last_activity_at:new Date().toISOString(),progress_mode:'STAGE',progress_value:0,execution_evidence:{assignment_id:assignmentId,architecture:'AADP-OS-V1.2-STOP-AWARE',asynchronous_submission:true}})});run=created[0];const tasks=await createTaskGraph(run.id as string);await recordEvent(run.id as string,null,'AADP_RUN_SUBMITTED','AADP publisher acquisition run submitted',{assignment_id:assignmentId,task_count:tasks.length});}EdgeRuntime.waitUntil(execute(run.id as string,a));return json({run_id:run.id,status:'QUEUED',asynchronous:true,poll:`command_runs?id=eq.${run.id}`},202);}catch(error){return json({error:error instanceof Error?error.message:String(error)},500);}});