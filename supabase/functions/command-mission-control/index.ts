import { corsHeaders, db, json, parseBody, requireDashboardAuth, invoke } from '../_shared/command.ts';
Deno.serve(async(request)=>{if(request.method==='OPTIONS')return new Response('ok',{headers:corsHeaders});const authError=await requireDashboardAuth(request);if(authError)return authError;try{const body=await parseBody(request)||{};const missionType=String(body.mission_type_key||'').trim().toUpperCase(),missionName=String(body.mission_name||'').trim(),agent=String(body.assigned_agent||'').trim(),stateCode=String(body.state_code||'').trim().toUpperCase()||null;if(!missionType||!missionName||!agent)return json({error:'Mission Type, Mission Name, and Agent are required.'},400);const type=(await db(`command_mission_types?mission_type_key=eq.${encodeURIComponent(missionType)}&enabled=eq.true&select=*`))?.[0];if(!type)return json({error:'Mission type is not enabled.'},400);if(type.state_context_required&&!stateCode)return json({error:'This mission type requires state context.'},400);if(!['PUBLISHER_DISCOVERY'].includes(missionType))return json({error:`${type.display_name} governance exists, but an autonomous runtime adapter is not yet deployed. No false mission was launched.`,code:'RUNTIME_ADAPTER_REQUIRED'},409);const now=new Date().toISOString();const run=(await db('command_runs',{method:'POST',body:JSON.stringify({idempotency_key:`ecc:${missionType}:${stateCode}:${crypto.randomUUID()}`,status:'running',current_stage:'INITIALIZING',definition_id:type.default_command_definition_id,aadp_state:'RUNNING',mission_type_key:missionType,mission_name:missionName,state_code:stateCode,assigned_agent:agent,last_activity_at:now,progress_mode:'STAGE',progress_value:5,execution_evidence:{source:'EXECUTIVE_COMMAND_CENTER',operator_authorized:true}})}))[0];const mission=(await db('command_missions',{method:'POST',body:JSON.stringify({mission_type_key:missionType,mission_name:missionName,state_code:stateCode,assigned_agent:agent,authorization_state:'AUTHORIZED',authorization_required:true,authorized_at:now,command_run_id:run.id,mission_config:{source:'EXECUTIVE_COMMAND_CENTER',defaults_resolved:true}})}))[0];let execution:any=null;try{execution=await invoke('command-aadp-publisher-discovery',{state_code:stateCode,mission_name:missionName,discovery_scope:'STATEWIDE_ALL',organization_types:['State Agencies','Counties','Cities / Municipalities','Universities','Community Colleges','School Districts','Transportation Authorities','Public Utilities','Water Districts','Special Districts','Public Authorities','Independent Agencies','Other Public Procurement Publishers'],provider:'system_default',operator:'Executive Command Center',notes:'Authorized through simplified Executive Mission Control.'});await db(`command_runs?id=eq.${run.id}`,{method:'PATCH',body:JSON.stringify({current_stage:execution.current_stage||'PUBLISHER_DISCOVERY_STARTED',last_activity_at:new Date().toISOString(),progress_value:10,execution_evidence:{source:'EXECUTIVE_COMMAND_CENTER',operator_authorized:true,discovery_run_id:execution.discovery_run_id}})});}catch(e){await db(`command_runs?id=eq.${run.id}`,{method:'PATCH',body:JSON.stringify({status:'failed',aadp_state:'FAILED',current_stage:'START_FAILED',failure_count:1,action_required:true,last_activity_at:new Date().toISOString(),result_summary:e.message})});throw e;}await db('command_audit_log',{method:'POST',body:JSON.stringify({entity_type:'MISSION',entity_id:mission.id,action_type:'MISSION_AUTHORIZED',actor_type:'OPERATOR',command_run_id:run.id,previous_state:{},new_state:{authorization_state:'AUTHORIZED',execution},reason:'Operator authorized mission from Executive Command Center',evidence:{mission_type:missionType,state_code:stateCode}})});return json({mission,run,execution},202);}catch(error){return json({error:error instanceof Error?error.message:String(error)},500)}});